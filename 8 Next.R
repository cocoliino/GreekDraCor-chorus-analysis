# 8 Multi-Chorus Subgraph Analysis
#
# goal:
# for every play with more than one chorus node, extract the induced subgraph
# of just the chorus nodes, then analyse the internal structure:
# - do chorus nodes actually co-occur (share scenes)?
# - how strong are inter-chorus edges vs chorus-character edges?
# - what is the density/topology of the chorus-only subgraph?